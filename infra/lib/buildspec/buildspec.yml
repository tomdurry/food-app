version: 0.2

phases:
  pre_build:
    commands:
      - apk add --no-cache nodejs npm
      - npm install -g cdktf-cli ts-node
      - cd infra
      - npm install

  build:
    commands:
      - cdktf deploy *stack --auto-approve

artifacts:
  files:
    - "**/*"
